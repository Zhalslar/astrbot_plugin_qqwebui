{
  "session_message_limit": {
    "description": "Recent message cache size per QQ session",
    "hint": "Recommended 100-500 for the first validation build.",
    "type": "int",
    "default": 200
  },
  "global_message_limit": {
    "description": "Global recent message cache size",
    "hint": "Older cached messages are evicted first when the limit is reached.",
    "type": "int",
    "default": 5000
  },
  "contact_ttl": {
    "description": "Friend and group metadata cache TTL in seconds",
    "hint": "Used to avoid repeated OneBot refresh calls.",
    "type": "int",
    "default": 300
  },
  "group_member_ttl": {
    "description": "Group member cache TTL in seconds",
    "hint": "Lower values refresh faster but cost more OneBot calls.",
    "type": "int",
    "default": 120
  }
}
